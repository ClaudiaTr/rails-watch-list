<h1>Here are all my lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to "#{list.name}", list_path(list.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a list", new_list_path, class: "btn-sm btn-dark"%>
