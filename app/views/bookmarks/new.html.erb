<h1>Bookmark the movie in a list</h1>

<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, prompt: 'Select a movie', input_html: { data: { controller: 'select2' } } %>
  <%= f.submit "Bookmark", class: "btn btn-dark" %>
<% end %>
